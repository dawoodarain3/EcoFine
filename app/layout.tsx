import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"
import { Header } from "@/components/header"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "EcoFine Products - Premium Microfiber Janitorial Products",
  description:
    "Global manufacturer of sustainable cleaning solutions and microfiber products for B2B clients and distributors.",
  keywords:
    "microfiber cloth manufacturer, eco-friendly cleaning products, janitorial OEM Asia, private label microfiber cloths, sustainable cleaning solutions",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          <Header />
          {children}
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  )
}
